<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    >
    <ImageView
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:id="@+id/iv_expand_tree"
        android:background="@drawable/ic_arrow_right"
        />
    <TextView
        android:id="@+id/tv_expand_tree"
        android:textSize="16dp"
        android:layout_centerVertical="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/iv_expand_tree"
        android:layout_toLeftOf="@+id/iv_expand_tree_check"
        android:layout_toEndOf="@+id/iv_expand_tree"
        android:layout_toStartOf="@+id/iv_expand_tree_check"
        android:text="现场类"/>
    <RadioButton
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginRight="4dp"
        android:id="@+id/iv_expand_tree_check"
        />
</RelativeLayout>